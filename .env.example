# ATRIA — environment configuration.
# Copy this to .env and edit as needed. Never commit .env itself.
# These are development defaults — never use them in production.

POSTGRES_PASSWORD=atria_dev
JWT_SECRET=atria_dev_secret_not_for_production
ATRIA_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8501
NEXT_PUBLIC_API_URL=http://localhost:8000
